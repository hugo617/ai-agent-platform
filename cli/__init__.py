"""agenthub CLI —— the Agent-to-Agent (AtoA) command-line surface.

A top-level package separate from the backend ``app/`` package. Installed as
the ``agenthub`` console script via ``[project.scripts]`` in pyproject.toml.
"""
