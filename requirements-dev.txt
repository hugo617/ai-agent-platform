-r requirements.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
aiosqlite==0.20.0  # in-memory SQLite for async tests
# moto mocks S3 so AmazonS3Storage is tested without real AWS credentials.
moto[s3]>=5.0,<6

# Lint / format
ruff==0.8.4
black==24.10.0
