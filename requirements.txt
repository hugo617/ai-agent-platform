# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.1 ; python_version < "3.13"
pydantic-settings>=2.7.1 ; python_version >= "3.13"
email-validator>=2.2,<3

# Database
SQLAlchemy==2.0.36
greenlet==3.1.1
alembic==1.14.0
psycopg[binary]==3.2.3
# casbin-sqlalchemy-adapter uses the sync psycopg2 driver
psycopg2-binary==2.9.10
pgvector==0.3.6

# Auth (verify Logto-issued JWT + local password login)
PyJWT==2.10.1
cryptography==44.0.0
httpx==0.28.1
bcrypt>=4.2,<5

# Authorization (pycasbin + SQLAlchemy adapter)
pycasbin==2.8.0
casbin-sqlalchemy-adapter==1.4.0

# Agent
langgraph==0.2.61
langchain-openai==0.2.14
langchain-core==0.3.28

# Utilities
python-dateutil==2.9.0.post0

# CLI (agenthub command — AtoA surface). The backend and CLI are packaged
# together in pyproject.toml (app* + cli*), so these runtime deps belong in the
# single source of truth that pip install -e . / pipx install -e . reads.
# click is imported directly by cli/errors.py (typer 0.12+ no longer vendors it).
typer>=0.12,<1
rich>=13,<14
click>=8.1,<9
